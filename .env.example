# =============================================================================
# TaskChute Cloud Retrospective Analysis Tool - 環境変数設定
# =============================================================================

# -----------------------------------------------------------------------------
# AWS/Bedrock設定 (AI分析機能を使用する場合に必要)
# -----------------------------------------------------------------------------
# AI分析機能 (--analyze) を使用する場合は以下を設定してください。
# AI分析を無効化する場合は --no-ai フラグを使用してください。

# AWSリージョン (デフォルト: us-east-1)
AWS_REGION=us-east-1

# AWS認証情報
# 注: ~/.aws/credentials ファイルで設定している場合は不要です
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# -----------------------------------------------------------------------------
# 使用例
# -----------------------------------------------------------------------------
# 1. .env.example を .env にコピー:
#    cp .env.example .env
#
# 2. 必要な環境変数を設定:
#    - AI分析を使う場合: AWS_* を設定
#    - 永続的プロファイルを使う場合: TASKCHUTE_* は不要
#
# 3. CLIを実行:
#    uv run python -m tccretro.cli --analyze
